export class User {
    constructor(
        private id: string,
        private email: string,
        private phone: string,
        private fullName: string,
        private role: string,
    ) {}
}